# 更新日志（Changelog）

本项目的全部重要变更都会记录在这里。版本号遵循语义化版本（SemVer）。

## [未发布]

### 界面（按设计稿 ui-01 / ui-02 / ui-03 落地）

- **家长仪表盘重排**：新增「写作业 / 网课 / 编程」三张书桌模板卡（写作业带「已断网」角标，
  点卡片直达守护设置）；「今日用量」升级为「每日屏幕时间」卡，圆环旁直接显示周内 / 周末
  两档额度；「最近七天用量」由横向列表改为竖向柱状图（七天七柱，悬停看明细）；「开始守护」
  与「奖励 15 分钟」移到底部主操作行。原有最近拦截、当前书桌、守护引擎、各软件用量卡片
  全部保留。
- **孩子书桌场景**：守护中的孩子视图改为蓝天渐变底，白名单软件以大磁贴卡片呈现（图标 +
  名称），倒计时与提示文案保持居中。
- **时间用完夜晚锁屏**：时间用完时孩子视图整体切换为夜晚渐变场景——月亮闹钟插画、
  「今天的时间用完啦」、加时说明与「申请加时」主按钮，角落保留「澄时守护中」与「睡觉时段」
  徽标；恢复后的书桌场景自动切回。
- **申请加时对话框**：标题对齐设计稿改为「申请加时」，新增「加时只对今天有效，跨天自动清零」
  脚注。

## [1.1.1] - 2026-08-31

### 修复（来自首次真机安装反馈）

- **引导设置崩溃（空引用）**：向导第 2 步的时长单选钮默认选中，XAML 解析期间就触发
  Checked 事件，此时同文件里更靠后的自定义时长面板还没创建，稳定崩溃。已按主窗口
  的 `_ready` 模式修复；该问题影响所有安装，属发布前必须拦截的缺陷。
- **界面连不上正在运行的守护服务**：管道对端校验读 SYSTEM 服务进程的
  `MainModule`，普通权限进程会被拒绝访问，导致应用永远回退到本机守护
  （断网/防强杀失效、配置只读）。改用低权限 `QueryFullProcessImageName` 校验。
- **首次设置后自动开始守护被门禁拦截**：服务模式下引导向导保存设置后立即开始
  守护会收到「先验证家长密码」。现在首次设置成功即视为该连接已授权（刚设出
  密码的人就是家长）；已配置机器的改设置门禁保持不变。
- **崩溃日志丢失**：数据目录被锁成只读后日志写不进去（静默失败）。现在自动退回
  `%LOCALAPPDATA%\Chengshi\logs`，引导设置等界面异常的弹窗也会提示日志位置。
- **安装脚本在 Windows 自带 PowerShell 5.1 下解析失败**（`?.` 语法）：安装器拉起
  的正是 5.1，payload 脚本已改为兼容写法，打包脚本新增 5.1 语法闸门防止复发。

## [1.1.0] - 2026-08-31

### 安全（商用前加固）

- **移除「我是家长」万能重置口令。** 旧版任何本机进程都能用这条固定口令
  重置家长密码、接管守护。现在找回码是唯一的密码重置凭证；找回码和密码
  都丢了，可由管理员卸载重装（原密码随之清除）。
- **密码哈希与找回码不再下发到界面进程。** 管道对本机所有进程开放，
  哈希泄露后 4 位数字密码可在几分钟内离线枚举。现在守护服务对
  `getConfig` 等应答做了脱敏；界面回存设置时服务自动保留密钥。
- **密码验证加入失败锁定（PinGate）。** 连错 5 次后指数退避（1 分钟起步，
  封顶 15 分钟），阻断在线穷举；同时校验时拒绝迭代数被篡改的哈希串。
- **「时间到」锁屏不再接受客户端通知。** 旧版孩子进程每 3 分钟伪造一条
  「已解锁」消息就能永久压住锁屏；现在解锁宽限只在守护服务端验证家长
  密码成功后开启。
- **邮箱找回密码全流程移入守护服务端。** 验证码只在服务端校验、绝不回传
  界面进程（旧版开发模式会把验证码直接弹在孩子屏幕上）；发码限流每分钟
  一次、验证码连错 10 次作废。
- **SMTP 授权码加密落盘。** 旧版明文存在 mail.json；现在用 Windows DPAPI
  加密，且 mail.json 只由守护服务写、配置读取不回传授权码。
- **数据目录 ACL 收紧真正生效。** 旧版 C# 代码按英文名匹配 SID（永远不
  命中），且不断开继承，等于没生效；现在按 SID 精确匹配、断开继承重授
  （Users 只读），服务启动与安装脚本（icacls /inheritance:r）双侧执行。
- **管道防抢注。** 服务端创建管道加 FILE_FLAG_FIRST_PIPE_INSTANCE；界面
  连接后核对管道对端进程是澄时服务，防止孩子进程先占管道名钓家长密码。
- **白名单堵住改名绕过。** 移除 cursor* 名字前缀与路径子串的万能放行；
  系统进程名（svchost/explorer/taskmgr 等）只有在镜像路径确实位于
  Windows 目录（Defender 引擎在其平台目录）时才放行，路径取不到一律
  拦截（失败关闭）。
- **配置文件失踪不再丢守护。** family.json 被删/写坏时守护服务用内存配置
  自愈，而不是把整个守护放弃掉。

### 修复

- UWP 应用按包家族名放行的规则此前是死代码（PFN 恒为 null），现在通过
  GetPackageFullName 正确解析，商店应用不再被误杀。
- ETW 进程监听中断后自动重启（每 30 秒重试），不再静默降级到纯轮询。

### 可靠性

- 界面与守护服务均接入文件日志（数据目录 logs\*.log，超限轮换）；
  WPF 增加全局异常处理（线程异常 + 未观察任务异常），不再只弹个框就吞掉。

### 分发

- 新增 `scripts\package.ps1` 一键打包：文件夹形态发布 → payload → 内嵌编译出
  `dist\ChengshiSetup.exe`（双击即装、自动提权），并附 `Chengshi-Setup-v版本.zip`
  （安装器 + EULA + 隐私政策）。
- 安装脚本补齐「设置 → 应用」卸载入口（ARP），卸载脚本会清理它。
- 统一 scripts 与 tools 两套安装器的目录布局与安全基线（SCM DACL、
  数据目录锁权、开机自启、快捷方式）。
- publish.ps1 支持用 `CHENGSHI_SIGN_PFX` / `CHENGSHI_SIGN_PWD` 自动做
  Authenticode 签名（未签名产物会明确警告）。
- 新增 EULA.md、PRIVACY.md、LICENSE；引导页提示「给孩子用标准用户账号」。

## [1.0.0] - 2026-08

首个对外版本：三种书桌模板、绿色上网（Chrome/Edge 策略）、睡觉时段断网、
每日限额（周内/周末/按星期）、申请加时、七天用量、家长密码 + 找回码、
SYSTEM 守护服务（ETW + 轮询双通道进程拦截）。
