<Window x:Class="Chengshi.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="澄时"
        Height="800"
        Width="1200"
        MinHeight="680"
        MinWidth="1080"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="#F5F4F1">
  <!--
    UI 全部由 wwwroot\（HTML/CSS/JS）经 WebView2 渲染。
    窗口本体只剩：无边框外壳 + 拖拽/缩放（WindowChrome）+ 网页宿主。
    标题栏拖拽区域由网页里的 app-region: drag 声明（IsNonClientRegionSupportEnabled）。
  -->
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="0"
                        ResizeBorderThickness="6"
                        GlassFrameThickness="0"
                        CornerRadius="0"
                        UseAeroCaptionButtons="False"/>
  </shell:WindowChrome.WindowChrome>

  <Border BorderBrush="#E7E4DD" BorderThickness="1">
    <wv2:WebView2 x:Name="Web" DefaultBackgroundColor="#F5F4F1"/>
  </Border>
</Window>
