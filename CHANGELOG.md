# 更新日志（Changelog）

本项目的全部重要变更都会记录在这里。版本号遵循语义化版本（SemVer）。

## [0.2.0] - 2026-09-08

### 新增

- **到点前分级提醒**：剩 10 / 5 / 1 分钟时，孩子书桌顶部弹出琥珀色提醒条
  （「还剩 10 分钟，快到时间了」→「准备收尾」→「最后 1 分钟」），到点不再搞突然袭击。
  提醒由界面按每秒推送的剩余时间计算，服务与本地守护模式一致。
- **「保存进度」宽限**：时间用完后先给 2 分钟缓冲——倒计时停在 0，书桌软件还能用来
  把存档、文档保存完，宽限结束才清场 + 锁屏；期间家长加时立即恢复倒计时，宽限本身
  不多扣额度。状态机以 `DeskSession.Grace` 一等公民实现，快照新增 `GraceRemaining`
  字段（IPC 按 camelCase 透传，老界面忽略该字段不受影响）。
- **锁屏 / 睡眠不扣时长**：屏幕锁定（Win+L / 切换用户）期间，每天总时长冻结、
  各软件用量暂停累计，解锁后接着算；睡眠 / 休眠本来就由单调时钟天然排除。
  探测走 WTS `WTSSessionInfoEx` 的会话锁定标志（服务与本机回退宿主默认启用），
  任何探测失败一律按「没锁」处理——只会多计不会少计，绝不白送时间；
  测试注入空探测器保证记账口径确定。

### 新增

- **「整个电脑」场景：只限时长、不限软件**。设置页「能用什么」的书桌里新增该选项：
  选中后不拦截任何软件和网站，守护只累计每天总时长、到点锁屏（次日自动恢复）。
  引擎侧以 `Desk.Unrestricted` 实现：拦截器对这类书桌一律放行，时间到仍走统一锁屏；
  用量统计照常记录全部前台进程。选择该场景时设置页自动收起「允许的软件」与
  「网站规则」区块，守护生效预览同步说明。
- **「使用统计」页：按软件的用量详情看板**。侧边栏新增入口，支持近 7 / 14 / 30 天切换：
  累计与日均时长、用过的软件数、拦截次数汇总；每天总时长柱图；应用时长排行（前 8 +
  其他，含占比）；每日明细表（总时长、拦截、用得最多的软件）。今天的数据实时并入。
- **用量日志开始按软件记录**：跨天落盘的用量流水（`usagelog.jsonl`）在原有的总时长、
  拦截次数之外，增加每个软件的分钟数。旧日志行没有该字段，统计页对那些天只显示总量，
  不受影响；新记录从下一次跨天开始积累。

### 变更

- **主界面迁移到 WebView2 混合架构（WPF 壳 + Web UI）**：MainWindow 的全部界面
  （仪表盘 / 守护设置 / 帮助、孩子的书桌与「时间用完」夜晚场景、护眼提醒）改由
  `wwwroot\` 下的 HTML/CSS/JS 经 WebView2 渲染，C# 侧保留守护引擎与配置逻辑，
  通过 `postMessage` 双向桥接（C# → JS 推状态，JS → C# 发命令）。视觉与交互升级：
  矢量图标替换手画线条、细滚动条、页面切换与悬停动效、标题栏拖拽区
  （app-region + IsNonClientRegionSupportEnabled）、孩子视图应用磁贴按名称着色。
  缺少 WebView2 运行时会给出官方下载指引；界面文件随安装目录分发，不依赖网络。
  PIN 弹窗、软件选择器、加时申请、引导设置、赞助窗口等模态对话框暂保留原生 XAML。
- 设计令牌延续原墨绿纸白配色，`App.xaml` 中的控件模板继续供保留的原生对话框使用。

### 新增

- **「支持澄时」赞助入口**：帮助页「关于」卡片新增按钮，弹出双栏收款码窗口
  （支付宝 / 微信支付）。澄时免费无广告，赞助全凭心意；图片随应用嵌入，不依赖网络。
- **帮助页「问题反馈」卡片**：位于「关于」卡片上方，提供复制反馈邮箱 / 打开邮件应用 /
  打开日志文件夹三个动作。邮件主题与正文自动带上澄时与 Windows 版本；日志文件夹与
  实际写日志去向一致（数据目录只读时同样回退到 LOCALAPPDATA）。

### 修复

- 帮助页「关于」的版本号写死为 1.0.0：改为运行时读取程序集版本，与实际发布号一致。

## [0.1.3] - 2026-09-05

### 修复

- **守护中倒计时冻结、反复弹「Unable to find an entry point named
  'WtsGetActiveConsoleSessionId' in DLL 'kernel32.dll'」**：个别精简版 / 老版 Windows 的
  kernel32 没有该导出，而每秒的守护 Tick 在 UI 线程上调用它，异常把整个刷新链打断。现在
  会话检测带降级链：kernel32 导出 → wtsapi32 枚举会话（取 State=Active）→ 当前进程会话；
  界面每秒刷新也加了兜底，单次失败记日志跳过、下一秒重试，不再冻结或连续弹窗。

## [0.1.2] - 2026-09-05

### 修复

- **覆盖安装 / 升级直接失败**：安装脚本在删除 Program Files 旧文件**之前**没有停止正在
  运行的守护服务与托盘进程，DLL 被占用导致「访问被拒绝」中止安装。现在安装前会先停服务、
  等待进程退出（最多 20 秒，强杀兜底）、再带 3 次重试地清理安装目录；`scripts\install.ps1`
  的「先复制后停服务」顺序一并修正。
- 升级 0.1.0 / 0.1.1：直接用本版安装器重装即可，无需手动清理；家长设置不受影响。

## [0.1.1] - 2026-09-05

### 新增

- **「电脑总时长」设置（不区分应用）**：把原来的「每天屏幕时间」正名为电脑总时长——不管
  孩子用哪个软件，每天累计用满就锁屏；周内 / 周末两档 + 按星期单独设的口径不变。
- **「开机后自动开始守护」开关**：把原本默认开启但看不见的 `GuardOnLaunch` 暴露到设置页。
  打开后（默认）电脑开机即自动开始守护，总时长从开机就计，不用家长手动点「开始守护」；
  关闭后恢复手动模式。开关随家长设置持久化，改动受家长密码门禁保护。

## [0.1.0] - 2026-09-05（首个公开发布版本）

> 0.1.0 之前为内部开发迭代（1.0.0–1.1.1），未对外分发。

### 界面（按 design/ 三张设计稿落地）

- **家长仪表盘重排**：新增「写作业 / 网课 / 编程」三张书桌模板卡（写作业带「已断网」角标，
  点卡片直达守护设置）；「今日用量」升级为「每日屏幕时间」卡，圆环旁直接显示周内 / 周末
  两档额度；「最近七天用量」由横向列表改为竖向柱状图（七天七柱，悬停看明细）；「开始守护」
  与「奖励 15 分钟」移到底部主操作行。原有最近拦截、当前书桌、守护引擎、各软件用量卡片
  全部保留。
- **孩子书桌场景**：守护中孩子视图独占整个窗口（侧边栏列宽归零），蓝天渐变底，白名单软件
  以大磁贴卡片呈现（图标 + 名称），倒计时与提示文案保持居中。
- **时间用完夜晚锁屏**：时间用完时全窗切换为夜晚渐变场景——月亮闹钟插画、「今天的时间用完
  啦」、加时说明与「申请加时」主按钮、描边版「找家长」按钮，角落保留「澄时守护中」与
  「睡觉时段」徽标；恢复后的书桌场景自动切回。
- **申请加时对话框**：标题改为「申请加时」，新增「加时只对今天有效，跨天自动清零」脚注。

### 修复

- **深色表面文字看不清**：全局 `TextBlock` 隐式样式把前景写死为墨色，压过按钮 / 导航模板的
  前景继承，侧边栏未选中项、主按钮、选中药丸上的文字近乎不可见。已移除该隐式前景
  （墨色改由窗口样式继承下发），导航文字改为显式绑定，未选中灰绿提亮。
- **选中药丸高度塌陷**：时长单选药丸在个别容器里排到只剩文字一半高。药丸样式增加
  `MinHeight` 兜底，「周一至周五 / 周末」切换行改用 `UniformGrid`。
- **卸载残留浏览器策略**：两个卸载脚本补上 Chrome / Edge 企业策略值（URLBlocklist /
  URLAllowlist / URLBlocklistEnabled）与断网防火墙规则的清理——此前守护中卸载（或崩溃
  残留）会让浏览器一直显示"由组织管理"。

## [1.1.1] - 2026-08-31

### 修复（来自首次真机安装反馈）

- **引导设置崩溃（空引用）**：向导第 2 步的时长单选钮默认选中，XAML 解析期间就触发
  Checked 事件，此时同文件里更靠后的自定义时长面板还没创建，稳定崩溃。已按主窗口
  的 `_ready` 模式修复；该问题影响所有安装，属发布前必须拦截的缺陷。
- **界面连不上正在运行的守护服务**：管道对端校验读 SYSTEM 服务进程的
  `MainModule`，普通权限进程会被拒绝访问，导致应用永远回退到本机守护
  （断网/防强杀失效、配置只读）。改用低权限 `QueryFullProcessImageName` 校验。
- **首次设置后自动开始守护被门禁拦截**：服务模式下引导向导保存设置后立即开始
  守护会收到「先验证家长密码」。现在首次设置成功即视为该连接已授权（刚设出
  密码的人就是家长）；已配置机器的改设置门禁保持不变。
- **崩溃日志丢失**：数据目录被锁成只读后日志写不进去（静默失败）。现在自动退回
  `%LOCALAPPDATA%\Chengshi\logs`，引导设置等界面异常的弹窗也会提示日志位置。
- **安装脚本在 Windows 自带 PowerShell 5.1 下解析失败**（`?.` 语法）：安装器拉起
  的正是 5.1，payload 脚本已改为兼容写法，打包脚本新增 5.1 语法闸门防止复发。

## [1.1.0] - 2026-08-31

### 安全（商用前加固）

- **移除「我是家长」万能重置口令。** 旧版任何本机进程都能用这条固定口令
  重置家长密码、接管守护。现在找回码是唯一的密码重置凭证；找回码和密码
  都丢了，可由管理员卸载重装（原密码随之清除）。
- **密码哈希与找回码不再下发到界面进程。** 管道对本机所有进程开放，
  哈希泄露后 4 位数字密码可在几分钟内离线枚举。现在守护服务对
  `getConfig` 等应答做了脱敏；界面回存设置时服务自动保留密钥。
- **密码验证加入失败锁定（PinGate）。** 连错 5 次后指数退避（1 分钟起步，
  封顶 15 分钟），阻断在线穷举；同时校验时拒绝迭代数被篡改的哈希串。
- **「时间到」锁屏不再接受客户端通知。** 旧版孩子进程每 3 分钟伪造一条
  「已解锁」消息就能永久压住锁屏；现在解锁宽限只在守护服务端验证家长
  密码成功后开启。
- **邮箱找回密码全流程移入守护服务端。** 验证码只在服务端校验、绝不回传
  界面进程（旧版开发模式会把验证码直接弹在孩子屏幕上）；发码限流每分钟
  一次、验证码连错 10 次作废。
- **SMTP 授权码加密落盘。** 旧版明文存在 mail.json；现在用 Windows DPAPI
  加密，且 mail.json 只由守护服务写、配置读取不回传授权码。
- **数据目录 ACL 收紧真正生效。** 旧版 C# 代码按英文名匹配 SID（永远不
  命中），且不断开继承，等于没生效；现在按 SID 精确匹配、断开继承重授
  （Users 只读），服务启动与安装脚本（icacls /inheritance:r）双侧执行。
- **管道防抢注。** 服务端创建管道加 FILE_FLAG_FIRST_PIPE_INSTANCE；界面
  连接后核对管道对端进程是澄时服务，防止孩子进程先占管道名钓家长密码。
- **白名单堵住改名绕过。** 移除 cursor* 名字前缀与路径子串的万能放行；
  系统进程名（svchost/explorer/taskmgr 等）只有在镜像路径确实位于
  Windows 目录（Defender 引擎在其平台目录）时才放行，路径取不到一律
  拦截（失败关闭）。
- **配置文件失踪不再丢守护。** family.json 被删/写坏时守护服务用内存配置
  自愈，而不是把整个守护放弃掉。

### 修复

- UWP 应用按包家族名放行的规则此前是死代码（PFN 恒为 null），现在通过
  GetPackageFullName 正确解析，商店应用不再被误杀。
- ETW 进程监听中断后自动重启（每 30 秒重试），不再静默降级到纯轮询。

### 可靠性

- 界面与守护服务均接入文件日志（数据目录 logs\*.log，超限轮换）；
  WPF 增加全局异常处理（线程异常 + 未观察任务异常），不再只弹个框就吞掉。

### 分发

- 新增 `scripts\package.ps1` 一键打包：文件夹形态发布 → payload → 内嵌编译出
  `dist\ChengshiSetup.exe`（双击即装、自动提权），并附 `Chengshi-Setup-v版本.zip`
  （安装器 + EULA + 隐私政策）。
- 安装脚本补齐「设置 → 应用」卸载入口（ARP），卸载脚本会清理它。
- 统一 scripts 与 tools 两套安装器的目录布局与安全基线（SCM DACL、
  数据目录锁权、开机自启、快捷方式）。
- publish.ps1 支持用 `CHENGSHI_SIGN_PFX` / `CHENGSHI_SIGN_PWD` 自动做
  Authenticode 签名（未签名产物会明确警告）。
- 新增 EULA.md、PRIVACY.md、LICENSE；引导页提示「给孩子用标准用户账号」。

## [1.0.0] - 2026-08

首个对外版本：三种书桌模板、绿色上网（Chrome/Edge 策略）、睡觉时段断网、
每日限额（周内/周末/按星期）、申请加时、七天用量、家长密码 + 找回码、
SYSTEM 守护服务（ETW + 轮询双通道进程拦截）。
